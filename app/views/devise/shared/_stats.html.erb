<% @user ||= current_user %>
<a href="<%= followings_user_path(@user) %>">
  <strong id="followings" class="stat"><%= @user.followings.count %></strong>
  following
</a>
<a href="<%= followers_user_path(@user) %>">
  <strong id="followers" class="stat"><%= @user.followers.count %></strong>
  followers
</a>